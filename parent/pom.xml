<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.springsource.insight</groupId>
    <artifactId>community</artifactId>
    <version>1.9.2.SR4</version>
    <name>com.springsource.insight:parent</name>
    <description>Spring Insight parent project. Defines dependencies and common configuration for the build process.</description>
    <packaging>pom</packaging>

    <properties>
        <insight.root.dir>${project.parent.basedir}/..</insight.root.dir>
			<!-- source code compliance level -->
		<java.source.version>1.5</java.source.version>
			<!-- target code compliance level -->
 		<java.target.version>${java.source.version}</java.target.version>
        <spring.framework.version>3.2.0.RELEASE</spring.framework.version>
        <spring.security.version>3.1.3.RELEASE</spring.security.version>
		<spring.webflow.version>2.3.1.RELEASE</spring.webflow.version>
        <spring.integration.version>2.2.0.RELEASE</spring.integration.version>
        <spring.data.commons.version>1.1.0.RELEASE</spring.data.commons.version>
        <aspectj.version>1.7.1</aspectj.version>
        <junit.version>4.10</junit.version>
        <mockito.version>1.9.0</mockito.version>
        <commons.logging.version>1.1.1</commons.logging.version>
        <log4j.version>1.2.17</log4j.version>
        <commons.math.version>2.1</commons.math.version>
        <commons.io.version>1.4</commons.io.version>
        <clover2.version>2.6.3</clover2.version>
        <slf4j.version>1.7.2</slf4j.version>
        <servlet-api.version>2.5</servlet-api.version>
		<jetty-api.version>6.1.25</jetty-api.version>
        <freemarker.version>2.3.15</freemarker.version>
        <jstl.version>1.2</jstl.version>
        <cglib.version>2.2</cglib.version>
        <activemq.version>5.4.2</activemq.version>
        <xbean.version>3.5</xbean.version>
        <catalina.version>6.0.18</catalina.version>
        <h2.version>1.2.147</h2.version>
        <hsqldb.version>1.8.0.10</hsqldb.version>
        <hibernate.version>3.6.0.Final</hibernate.version>
        <commons.dbcp.version>1.2.2</commons.dbcp.version>
        <liquibase.version>2.0.1</liquibase.version>
        <gemfire.version>6.5.1</gemfire.version>
        <spring.gemfire.version>1.0.0.RELEASE</spring.gemfire.version>
        <ehcache.version>2.3.0</ehcache.version>
        <groovy.version>1.7.3</groovy.version>
        <gmaven.plugin.version>1.0</gmaven.plugin.version>
		<surefire.plugin.version>2.12.2</surefire.plugin.version>
        <injection.plugin.version>1.0.2</injection.plugin.version>
        <vfabric.license.client.version>1.0.0.M8</vfabric.license.client.version>
        <aspectj.maven.version>1.2</aspectj.maven.version>
        <rabbitmq.version>2.5.1</rabbitmq.version>
        <shade.plugin.version>1.3.3</shade.plugin.version>
        <jackson.version>1.9.9</jackson.version>
        <org.springframework.amqp.version>1.0.0.RELEASE</org.springframework.amqp.version>
        <commons.codec.version>1.5</commons.codec.version>
        <json-lib.version>2.2.3</json-lib.version>
        <!-- maven-rpm-plugin .. while not the newest, does not have bugs the newer versions have -->
        <rpm.version>2.0-beta-4</rpm.version>
        <cloudfoundry.version>0.7.0</cloudfoundry.version>
        <httpclinet.version>4.1.1</httpclinet.version>
        <javax.jms.version>1.1</javax.jms.version>
		<javax.inject.version>1</javax.inject.version>

        <maven.build.timestamp.format>yyyyMMddHHmmss</maven.build.timestamp.format>
        <build.timestamp>${maven.build.timestamp}</build.timestamp>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

		<excluded.tests.group>com.springsource.insight.util.test.MicroBenchmark</excluded.tests.group>
		
		<snapshot.repo.id>spring-snapshot</snapshot.repo.id>
        <snapshot.repo.name>Spring Snapshot Repository</snapshot.repo.name>
		<snapshot.repo.url>http://private.maven.springsource.com/snapshot</snapshot.repo.url>
        <snapshot.dist.url>s3://private.maven.springsource.com/snapshot</snapshot.dist.url>

        <milestone.repo.id>spring-milestone</milestone.repo.id>
        <milestone.repo.name>Spring Milestone Repository</milestone.repo.name>
        <milestone.repo.url>http://private.maven.springsource.com/milestone</milestone.repo.url>
        <milestone.dist.url>s3://private.maven.springsource.com/milestone</milestone.dist.url>

		<release.repo.id>spring-release</release.repo.id>
        <release.repo.name>Spring Release Repository</release.repo.name>
        <release.repo.url>http://private.maven.springsource.com/release</release.repo.url>
        <release.dist.url>s3://private.maven.springsource.com/release</release.dist.url>
    </properties>

    <profiles>
		<profile>
			<id>force-deploy-artifacts</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-deploy-plugin</artifactId>
							<!-- NOTE: by default we deploy only 1.5 compiled artifacts -->
						<configuration>
							<skip>false</skip>
						</configuration>
					</plugin>            
				</plugins>
			</build>
		</profile>
		<profile>
			<id>with-micro-benchmarks</id>
			<properties>
				<excluded.tests.group>java.lang.String</excluded.tests.group>
			</properties>
		</profile>

        <profile>
            <id>fast</id>
            <properties>
                <skipTests>true</skipTests>
                <maven.javadoc.skip>true</maven.javadoc.skip>
            </properties>
        </profile>

        <profile>
            <id>enable-private-snapshots</id>
            <repositories>
		        <repository>	<!-- NOTE: these values must match the properties -->
		            <id>spring-public-snapshot</id>
		            <name>Spring Public Snapshot Repository</name>
		            <url>http://private.maven.springsource.com/snapshot</url>
		            <snapshots>
		                <enabled>true</enabled>
		            </snapshots>
		            <releases>
		                <updatePolicy>never</updatePolicy>
		            </releases>
		        </repository>
            </repositories>
        </profile>

        <profile>
            <id>mark-scm-revision</id>
            <build>
                <plugins>
		            <plugin>
		                <groupId>org.codehaus.mojo</groupId>
		                <artifactId>buildnumber-maven-plugin</artifactId>
		                <executions>
		                    <execution>
		                        <phase>validate</phase>
		                        <goals>
		                            <goal>create</goal>
		                        </goals>
		                        <configuration>
		                             <getRevisionOnlyOnce>true</getRevisionOnlyOnce>
		                             <revisionOnScmFailure>NO-SHA-1</revisionOnScmFailure>
		                        </configuration>
		                    </execution>
		                </executions>
		            </plugin>
		            <plugin>
		                <groupId>org.apache.maven.plugins</groupId>
		                <artifactId>maven-jar-plugin</artifactId>
		                <configuration>
		                    <archive>
		                        <!-- Extra attributes added to the MANIFEST.MF file -->
		                        <manifestEntries>
		                            <SCM-Revision-Id>${buildNumber}</SCM-Revision-Id>
		                        </manifestEntries>
		                    </archive>
		                </configuration>
		            </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-war-plugin</artifactId>
                        <configuration>
                            <archive>
                                <!-- Extra attributes added to the MANIFEST.MF file -->
                                <manifestEntries>
                                    <SCM-Revision-Id>${buildNumber}</SCM-Revision-Id>
                                </manifestEntries>
                            </archive>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                        <configuration>
                            <archive>
                                <!-- Extra attributes added to the MANIFEST.MF file -->
                                <manifestEntries>
                                    <SCM-Revision-Id>${buildNumber}</SCM-Revision-Id>
                                </manifestEntries>
                            </archive>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>

		<profile>
			<id>with-javadoc</id>
            <build>
                <plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-javadoc-plugin</artifactId>
						<executions>
							<execution>
								<goals>
									<goal>jar</goal>
								</goals>
							</execution>
						</executions>
                    </plugin>
                </plugins>
            </build>
		</profile>
    </profiles>

    <organization>
        <name>VMware, Inc.</name>
        <url>http://springsource.org/insight</url>
    </organization>

    <repositories>
        <repository>	<!-- NOTE: these values must match the properties -->
            <id>spring-release</id>
            <name>Spring Release Repository</name>
            <url>http://maven.springframework.org/release</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
            <releases>
                <updatePolicy>never</updatePolicy>
            </releases>
        </repository>
        <repository>	<!-- NOTE: these values must match the properties -->
            <id>spring-milestone</id>
            <name>Spring Milestone Repository</name>
            <url>http://maven.springframework.org/milestone</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
            <releases>
                <updatePolicy>never</updatePolicy>
            </releases>
        </repository>
        <repository>
            <id>gemfire-release-repo</id>
            <name>Gemfire Release Repository</name>
            <url>http://dist.gemstone.com/maven/release</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
            <releases>
                <updatePolicy>never</updatePolicy>
            </releases>
        </repository>
        <repository>
            <id>hyperic-external-repo</id>
            <name>Hyperic External Repository</name>
            <url>http://maven.hyperic.org/external</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
            <releases>
                <updatePolicy>never</updatePolicy>
            </releases>
        </repository>
        <repository>
            <id>org.jboss.maven.release.nexus</id>
            <name>JBoss Maven Central-compatible Repository</name>
            <url>http://repository.jboss.org/nexus/content/groups/public</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
            <releases>
                <updatePolicy>never</updatePolicy>
            </releases>
        </repository>
        <repository>
            <id>org.jboss.maven.release</id>
            <name>JBoss Maven Central-compatible Repository</name>
            <url>http://repository.jboss.com/maven2</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
            <releases>
                <updatePolicy>never</updatePolicy>
            </releases>
        </repository>
        <repository>
            <id>jboss-deprecated</id>
            <name>JBoss Deprecated - Used to get some JBoss depends (jpl-pattern)</name>
            <url>https://repository.jboss.org/nexus/content/repositories/deprecated/</url>
            <layout>default</layout>
            <releases>
                <enabled>true</enabled>
                <updatePolicy>never</updatePolicy>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
    </repositories>

    <pluginRepositories>
        <pluginRepository>	<!-- NOTE: these values must match the properties -->
            <id>spring-release</id>
            <name>Spring Release Repository</name>
            <url>http://maven.springframework.org/release</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
            <releases>
                <updatePolicy>never</updatePolicy>
            </releases>
        </pluginRepository>
        <!-- Milestone repository for the aspectj milestone release -->
        <pluginRepository>	<!-- NOTE: these values must match the properties -->
            <id>spring-milestone</id>
            <name>Spring Milestone Repository</name>
            <url>http://maven.springframework.org/milestone</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
            <releases>
                <updatePolicy>never</updatePolicy>
            </releases>
        </pluginRepository>
        <pluginRepository>
            <id>org.jboss.maven.release.nexus</id>
            <name>JBoss Maven Central-compatible Repository</name>
            <url>http://repository.jboss.org/nexus/content/groups/public</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
            <releases>
                <updatePolicy>never</updatePolicy>
            </releases>
        </pluginRepository>
        <pluginRepository>
            <id>codehaus central</id>
            <name>Codehaus Central Repository</name>
            <url>http://repository.codehaus.org</url>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
            <releases>
                <updatePolicy>never</updatePolicy>
            </releases>
        </pluginRepository>
    </pluginRepositories>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>junit</groupId>
				<artifactId>junit</artifactId>
				<version>${junit.version}</version>
			</dependency>

			<dependency>
				<groupId>org.mockito</groupId>
				<artifactId>mockito-all</artifactId>
				<version>${mockito.version}</version>
			</dependency>

			<dependency>
				<groupId>org.hsqldb</groupId>
				<artifactId>hsqldb</artifactId>
				<version>${hsqldb.version}</version>
			</dependency>

	        <dependency>
				<groupId>commons-io</groupId>
				<artifactId>commons-io</artifactId>
				<version>${commons.io.version}</version>
			</dependency>
			<dependency>
				<groupId>commons-dbcp</groupId>
				<artifactId>commons-dbcp</artifactId>
				<version>${commons.dbcp.version}</version>
			</dependency>       
			<dependency>
				<groupId>commons-logging</groupId>
				<artifactId>commons-logging</artifactId>
				<version>${commons.logging.version}</version>
			</dependency>

			<dependency>
				<groupId>javax.servlet</groupId>
				<artifactId>servlet-api</artifactId>
				<version>${servlet-api.version}</version>
			</dependency>
			<dependency>
				<groupId>javax.jms</groupId>
				<artifactId>jms</artifactId>
				<version>${javax.jms.version}</version>
			</dependency>
			<dependency> <!-- used by Spring -->
				<groupId>javax.inject</groupId>
				<artifactId>javax.inject</artifactId>
				<version>${javax.inject.version}</version>
			</dependency>
			<dependency>
				<groupId>javax.servlet</groupId>
				<artifactId>jstl</artifactId>
				<version>${jstl.version}</version>
			</dependency>
			<dependency>
				<groupId>javax.servlet.jsp</groupId>
				<artifactId>jsp-api</artifactId>
				<version>2.2</version>
			</dependency>

	        <dependency>
				<groupId>org.aspectj</groupId>
				<artifactId>aspectjweaver</artifactId>
				<version>${aspectj.version}</version>
			</dependency>
	        <dependency>
				<groupId>org.aspectj</groupId>
				<artifactId>aspectjrt</artifactId>
				<version>${aspectj.version}</version>
			</dependency>
	        <dependency>
				<groupId>org.aspectj</groupId>
				<artifactId>aspectjtools</artifactId>
				<version>${aspectj.version}</version>
			</dependency>

	        <dependency>
				<groupId>org.mortbay.jetty</groupId>
				<artifactId>jetty</artifactId>
				<version>${jetty-api.version}</version>
				<exclusions>
				    <exclusion>	<!-- we provide our own -->
						<groupId>org.mortbay.jetty</groupId>
						<artifactId>servlet-api</artifactId>
				    </exclusion>
				</exclusions>
			</dependency>
	        <dependency>
				<groupId>org.mortbay.jetty</groupId>
				<artifactId>jetty-util</artifactId>
				<version>${jetty-api.version}</version>
				<exclusions>
				    <exclusion>	<!-- we provide our own -->
						<groupId>org.mortbay.jetty</groupId>
						<artifactId>servlet-api</artifactId>
				    </exclusion>
				</exclusions>
			</dependency>

			<dependency> <!-- Required also by various frameworks (e.g., Spring) -->
				<groupId>org.slf4j</groupId>
				<artifactId>slf4j-api</artifactId>
				<version>${slf4j.version}</version>
			</dependency>
				<!-- Replacement of commons-logging with a native slf4j 
					 - Required by Spring framework (and others) -->
			<dependency>
				<groupId>org.slf4j</groupId>
				<artifactId>jcl-over-slf4j</artifactId>
				<version>${slf4j.version}</version>
			</dependency>
			<dependency> <!-- Required also by various frameworks (e.g., Spring) -->
				<groupId>org.slf4j</groupId>
				<artifactId>slf4j-simple</artifactId>
				<version>${slf4j.version}</version>
			</dependency>
			<dependency>
				<groupId>org.slf4j</groupId>
				<artifactId>slf4j-log4j12</artifactId>
				<version>${slf4j.version}</version>
			</dependency>
			<dependency>
				<groupId>org.slf4j</groupId>
				<artifactId>jul-to-slf4j</artifactId>
				<version>${slf4j.version}</version>
			</dependency>
			<dependency>
				<groupId>org.slf4j</groupId>
				<artifactId>slf4j-nop</artifactId>
				<version>${slf4j.version}</version>
			</dependency>
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>log4j-over-slf4j</artifactId>
                <version>${slf4j.version}</version>
            </dependency>

			<dependency>
				<groupId>log4j</groupId>
				<artifactId>log4j</artifactId>
				<version>${log4j.version}</version>
				<exclusions>
					<exclusion>
						<groupId>javax.jms</groupId>
						<artifactId>jms</artifactId>
					</exclusion>
					<exclusion>
						<groupId>com.sun.jmx</groupId>
						<artifactId>jmxri</artifactId>
					</exclusion>
					<exclusion>
						<groupId>com.sun.jdmk</groupId>
						<artifactId>jmxtools</artifactId>
					</exclusion>
					<exclusion>
						<groupId>ant</groupId>
						<artifactId>ant-nodeps</artifactId>
					</exclusion>
					<exclusion>
						<groupId>ant</groupId>
						<artifactId>ant-junit</artifactId>
					</exclusion>
					<exclusion>
						<groupId>ant-contrib</groupId>
						<artifactId>ant-contrib</artifactId>
					</exclusion>
				</exclusions>
			</dependency>

			<dependency>
				<groupId>org.codehaus.jackson</groupId>
				<artifactId>jackson-core-asl</artifactId>
				<version>${jackson.version}</version>
			</dependency>
			<dependency>
				<groupId>org.codehaus.jackson</groupId>
				<artifactId>jackson-mapper-asl</artifactId>
				<version>${jackson.version}</version>
			</dependency>

			<dependency>
				<groupId>org.freemarker</groupId>
				<artifactId>freemarker</artifactId>
				<version>${freemarker.version}</version>
			</dependency>

			<dependency>
				<groupId>org.hibernate</groupId>
				<artifactId>hibernate-entitymanager</artifactId>
				<version>${hibernate.version}</version>
			</dependency>
			<dependency>
				<groupId>org.hibernate</groupId>
				<artifactId>hibernate-core</artifactId>
				<version>${hibernate.version}</version>
			</dependency>        

			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-core</artifactId>
				<version>${spring.framework.version}</version>
				<exclusions>
					<exclusion> <!-- Provided by the J2SE -->
						<groupId>xml-apis</groupId>
						<artifactId>xml-apis</artifactId>
					</exclusion>
						<!-- excluded since we provide our
							own dependencies versions for them - 
							usually jcl-over-slf4j	-->
					<exclusion>
						<groupId>commons-logging</groupId>
						<artifactId>commons-logging</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-aop</artifactId>
				<version>${spring.framework.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-aspects</artifactId>
				<version>${spring.framework.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-beans</artifactId>
				<version>${spring.framework.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-context</artifactId>
				<version>${spring.framework.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-context-support</artifactId>
				<version>${spring.framework.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-expression</artifactId>
				<version>${spring.framework.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-instrument</artifactId>
				<version>${spring.framework.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-jdbc</artifactId>
				<version>${spring.framework.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-jms</artifactId>
				<version>${spring.framework.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-orm</artifactId>
				<version>${spring.framework.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-oxm</artifactId>
				<version>${spring.framework.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-tx</artifactId>
				<version>${spring.framework.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-web</artifactId>
				<version>${spring.framework.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-webmvc</artifactId>
				<version>${spring.framework.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-test</artifactId>
				<version>${spring.framework.version}</version>
			</dependency>

	        <dependency>
	            <groupId>org.springframework.webflow</groupId>
	            <artifactId>spring-webflow</artifactId>
	            <version>${spring.webflow.version}</version>
	            <exclusions>
	                <exclusion> <!-- we use jcl-over-slf4j in the tests -->
	                    <groupId>commons-logging</groupId>
	                    <artifactId>commons-logging</artifactId>
	                </exclusion>
	            </exclusions>
	        </dependency>
            <dependency>
                <groupId>org.springframework.webflow</groupId>
                <artifactId>spring-binding</artifactId>
                <version>${spring.webflow.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework.webflow</groupId>
                <artifactId>spring-js</artifactId>
                <version>${spring.webflow.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework.webflow</groupId>
                <artifactId>spring-js-resources</artifactId>
                <version>${spring.webflow.version}</version>
            </dependency>

	        <dependency>
	            <groupId>org.springframework.integration</groupId>
	            <artifactId>spring-integration-core</artifactId>
	            <version>${spring.integration.version}</version>
	        </dependency>
            <dependency>
                <groupId>org.springframework.integration</groupId>
                <artifactId>spring-integration-ip</artifactId>
                <version>${spring.integration.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework.integration</groupId>
                <artifactId>spring-integration-stream</artifactId>
                <version>${spring.integration.version}</version>
            </dependency>
	        <dependency>
	            <groupId>org.springframework.integration</groupId>
	            <artifactId>spring-integration-jms</artifactId>
	            <version>${spring.integration.version}</version>
	        </dependency>
            <dependency>
                <groupId>org.springframework.integration</groupId>
                <artifactId>spring-integration-jmx</artifactId>
                <version>${spring.integration.version}</version>
            </dependency>
	        <dependency>
	            <groupId>org.springframework.integration</groupId>
	            <artifactId>spring-integration-http</artifactId>
	            <version>${spring.integration.version}</version>
	        </dependency>
            <dependency>
                <groupId>org.springframework.integration</groupId>
                <artifactId>spring-integration-test</artifactId>
                <version>${spring.integration.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework.integration</groupId>
                <artifactId>spring-integration-mail</artifactId>
                <version>${spring.integration.version}</version>
            </dependency>

			<dependency>
				<groupId>org.springframework.security</groupId>
				<artifactId>spring-security-core</artifactId>
				<version>${spring.security.version}</version>
				<exclusions>
						<!-- we use our own versions for these -->
					<exclusion>
						<groupId>org.aspectj</groupId>
						<artifactId>aspectjrt</artifactId>
					</exclusion>
						<!-- excluded since we provide our
							own dependencies versions for them - 
							usually jcl-over-slf4j	-->
					<exclusion>
						<groupId>commons-logging</groupId>
						<artifactId>commons-logging</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>org.springframework.security</groupId>
				<artifactId>spring-security-config</artifactId>
				<version>${spring.security.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework.security</groupId>
				<artifactId>spring-security-web</artifactId>
				<version>${spring.security.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework.security</groupId>
				<artifactId>spring-security-ldap</artifactId>
				<version>${spring.security.version}</version>
					<!-- we use our own versions for these -->
				<exclusions>
					<exclusion>
						<groupId>org.aspectj</groupId>
						<artifactId>aspectjrt</artifactId>
					</exclusion>
					<exclusion>
						<groupId>org.aspectj</groupId>
						<artifactId>aspectjtools</artifactId>
					</exclusion>
					<exclusion>
						<groupId>org.aspectj</groupId>
						<artifactId>aspectjweaver</artifactId>
					</exclusion>
				</exclusions>
			</dependency>

			<!-- amqp -->
			<dependency>
				<groupId>org.springframework.amqp</groupId>
				<artifactId>spring-amqp</artifactId>
				<version>${org.springframework.amqp.version}</version>
			</dependency>

				<!-- Rabbitmq -->
			<dependency>
				<groupId>com.rabbitmq</groupId>
				<artifactId>amqp-client</artifactId>
				<version>${rabbitmq.version}</version>
			</dependency>

			<dependency>
				<groupId>org.springframework.amqp</groupId>
				<artifactId>spring-rabbit</artifactId>
				<version>${org.springframework.amqp.version}</version>
			</dependency>

			<dependency>
				<groupId>org.springframework.data</groupId>
				<artifactId>spring-data-commons-core</artifactId>
				<version>${spring.data.commons.version}</version>
			</dependency>
		</dependencies>
	</dependencyManagement>
		
    <build>
        <extensions>
            <extension>
                <groupId>org.springframework.build.aws</groupId>
                <artifactId>org.springframework.build.aws.maven</artifactId>
                <version>3.0.0.RELEASE</version>
            </extension>
        </extensions>
        <pluginManagement>
            <plugins>
                <!--This plugin's configuration is used to store Eclipse 
                    m2e settings only. It has no influence on the Maven build itself. -->
                <plugin>
                    <groupId>org.eclipse.m2e</groupId>
                    <artifactId>lifecycle-mapping</artifactId>
                    <version>1.0.0</version>
                    <configuration>
                        <lifecycleMappingMetadata>
                            <pluginExecutions>
                                    <!-- TODO remove this definition when same gmaven
                                        plugin used throughout (see below) -->
                                <pluginExecution>
                                    <pluginExecutionFilter>
                                        <groupId>org.codehaus.groovy.maven</groupId>
                                        <artifactId>gmaven-plugin</artifactId>
                                        <versionRange>[1.0,)</versionRange>
                                        <goals>
                                            <goal>execute</goal>
                                            <goal>compile</goal>
                                            <goal>testCompile</goal>
                                            <goal>generateStubs</goal>
                                            <goal>generateTestStubs</goal>
                                        </goals>
                                    </pluginExecutionFilter>
                                    <action>
                                        <execute>
                                            <runOnIncremental>false</runOnIncremental>
                                        </execute>
                                    </action>
                                </pluginExecution>

                                <pluginExecution>
                                    <pluginExecutionFilter>
                                        <groupId>org.codehaus.gmaven</groupId>
                                        <artifactId>gmaven-plugin</artifactId>
                                        <versionRange>[1.0,)</versionRange>
                                        <goals>
                                            <goal>execute</goal>
                                            <goal>compile</goal>
                                            <goal>testCompile</goal>
                                            <goal>generateStubs</goal>
                                            <goal>generateTestStubs</goal>
                                        </goals>
                                    </pluginExecutionFilter>
                                    <action>
                                        <execute>
                                            <runOnIncremental>false</runOnIncremental>
                                        </execute>
                                    </action>
                                </pluginExecution>

                                <pluginExecution>
                                    <pluginExecutionFilter>
                                        <groupId>org.codehaus.mojo</groupId>
                                        <artifactId>aspectj-maven-plugin</artifactId>
                                        <versionRange>[1.0,)</versionRange>
                                        <goals>
                                            <goal>compile</goal>
                                            <goal>test-compile</goal>
                                        </goals>
                                    </pluginExecutionFilter>
                                    <action>
                                        <execute>
                                            <runOnIncremental>false</runOnIncremental>
                                        </execute>
                                    </action>
                                </pluginExecution>

                                <pluginExecution>
                                    <pluginExecutionFilter>
                                        <groupId>com.keyboardsamurais.maven</groupId>
                                        <artifactId>maven-timestamp-plugin</artifactId>
                                        <versionRange>[1.0,)</versionRange>
                                        <goals>
                                            <goal>create</goal>
                                        </goals>
                                    </pluginExecutionFilter>
                                    <action>
                                        <execute>
                                            <runOnIncremental>false</runOnIncremental>
                                        </execute>
                                    </action>
                                </pluginExecution>

                                <pluginExecution>
                                    <pluginExecutionFilter>
                                        <groupId>org.jboss.maven.plugins</groupId>
                                        <artifactId>maven-injection-plugin</artifactId>
                                        <versionRange>[1.0.2,)</versionRange>
                                        <goals>
                                            <goal>bytecode</goal>
                                        </goals>
                                    </pluginExecutionFilter>
                                    <action>
                                        <execute>
                                            <runOnIncremental>false</runOnIncremental>
                                        </execute>
                                    </action>
                                </pluginExecution>

                                <pluginExecution>
                                    <pluginExecutionFilter>
                                        <groupId>org.apache.maven.plugins</groupId>
                                        <artifactId>maven-antrun-plugin</artifactId>
                                        <versionRange>[1.6,)</versionRange>
                                        <goals>
                                            <goal>run</goal>
                                        </goals>
                                    </pluginExecutionFilter>
                                    <action>
                                        <execute>
                                            <runOnIncremental>false</runOnIncremental>
                                        </execute>
                                    </action>
                                </pluginExecution>
                            </pluginExecutions>
                        </lifecycleMappingMetadata>
                    </configuration>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-assembly-plugin</artifactId>
                    <version>2.2.1</version>
                    <configuration>
                        <appendAssemblyId>false</appendAssemblyId>
                        <descriptors>
                            <descriptor>src/main/assembly/assembly.xml</descriptor>
                        </descriptors>
                    </configuration>
                    <executions>
                        <execution>
                            <id>package</id>
                            <phase>package</phase>
                            <goals>
                                <goal>single</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-shade-plugin</artifactId>
                    <version>${shade.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-clean-plugin</artifactId>
                    <version>2.4.1</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-install-plugin</artifactId>
                    <version>2.3.1</version>
                </plugin>
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>aspectj-maven-plugin</artifactId>
                    <version>1.4</version>
                    <!-- As per Andy Clement's recommendation and also as per the plugin's documentation -->
                    <dependencies>
                        <dependency>
                            <groupId>org.aspectj</groupId>
                            <artifactId>aspectjrt</artifactId>
                            <version>${aspectj.version}</version>
                        </dependency>
                        <dependency>
                            <groupId>org.aspectj</groupId>
                            <artifactId>aspectjtools</artifactId>
                            <version>${aspectj.version}</version>
                        </dependency>
                    </dependencies>
					<configuration>
						<source>${java.source.version}</source>
						<target>${java.target.version}</target>
						<outxml>true</outxml>
					</configuration>
 					<executions>
						<execution>
							<id>compile-aspects</id>
							<goals>
								<goal>compile</goal>
							</goals>
						</execution>
						<execution>
							<id>compile-test-aspects</id>
							<!-- see http://jira.codehaus.org/browse/MASPECTJ-92 workaround -->
							<phase>process-test-sources</phase>
							<goals>
								<goal>test-compile</goal>
							</goals>
						</execution>
					</executions>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-war-plugin</artifactId>
                    <version>2.3</version>
                    <configuration>
                        <packagingExcludes>WEB-INF/web.xml</packagingExcludes>
                        <webXml>src/main/webapp/WEB-INF/web.xml</webXml>
	                    <archive>
	                        <!-- Extra attributes added to the MANIFEST.MF file -->
	                        <manifestEntries>
	                            <!-- see Package constructor using Manifest -->
	                            <Implementation-Title>${project.artifactId}</Implementation-Title>
	                            <Implementation-Version>${project.version}</Implementation-Version>
	                            <Implementation-Vendor>Insight</Implementation-Vendor>
	                               <!-- NOTE: this name must match the one used in InsightVersion class -->
	                            <Release-Timestamp>${buildtimestamp}</Release-Timestamp>
	                        </manifestEntries>
	                    </archive>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>com.atlassian.maven.plugins</groupId>
                    <artifactId>maven-clover2-plugin</artifactId>
                    <version>${clover2.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.jboss.maven.plugins</groupId>
                    <artifactId>maven-injection-plugin</artifactId>
                    <version>${injection.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>com.keyboardsamurais.maven</groupId>
                    <artifactId>maven-timestamp-plugin</artifactId>
                    <version>1.0</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-antrun-plugin</artifactId>
                    <version>1.6</version>
                </plugin>
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>rpm-maven-plugin</artifactId>
                    <version>${rpm.version}</version>
                </plugin>
                <plugin>
                    <groupId>com.mycila.maven-license-plugin</groupId>
                    <artifactId>maven-license-plugin</artifactId>
                    <version>1.9.0</version>
                    <configuration>
                        <header>${project.parent.basedir}/aslHeader.txt</header>
                        <includes>
                            <include>**/*.java</include>
                            <include>**/*.aj</include>
                        </includes>
                        <properties>
                            <year>2009-2011</year>
                        </properties>
                    </configuration>
                    <executions>
                        <execution>
                            <goals>
                                <goal>check</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>2.5</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-scm-plugin</artifactId>
                    <version>1.7</version>
                </plugin>
	            <plugin>
	                <groupId>org.codehaus.mojo</groupId>
	                <artifactId>buildnumber-maven-plugin</artifactId>
	                <version>1.1</version>
                </plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-javadoc-plugin</artifactId>
					<version>2.9</version>
					<configuration>
	                    <archive>
	                        <!-- Extra attributes added to the MANIFEST.MF file -->
	                        <manifestEntries>
	                            <!-- see Package constructor using Manifest -->
	                            <Implementation-Title>${project.artifactId}</Implementation-Title>
	                            <Implementation-Version>${project.version}</Implementation-Version>
	                            <Implementation-Vendor>Insight</Implementation-Vendor>
	                               <!-- NOTE: this name must match the one used in InsightVersion class -->
	                            <Release-Timestamp>${buildtimestamp}</Release-Timestamp>
	                        </manifestEntries>
	                    </archive>
						<quiet>true</quiet>
						<links>
							<link>http://docs.mockito.googlecode.com/hg/${mockito.version}</link>
							<link>http://kentbeck.github.com/junit/javadoc/${junit.version}</link>
						</links>
					</configuration>
				</plugin>
            </plugins>
        </pluginManagement>
        <plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-enforcer-plugin</artifactId>
				<version>1.1.1</version>
				<executions>
					<execution>
						<id>enforce-versions</id>
						<goals>
							<goal>enforce</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<rules>
						<requireMavenVersion>
							<version>[3.0,)</version>
						</requireMavenVersion>
					</rules>
				</configuration>
			</plugin>
            <plugin>
                <groupId>org.codehaus.groovy.maven</groupId>
                <artifactId>gmaven-plugin</artifactId>
                <version>${gmaven.plugin.version}</version>
                <executions>
                    <!-- Works only for Maven 3.x -->
					<execution>
                        <id>detect-insight-root-dir</id>
                        <phase>initialize</phase>
                        <goals>
                            <goal>execute</goal>
                        </goals>
                        <configuration>
                            <source>
							<![CDATA[
                                for (java.io.File file=new java.io.File(new java.net.URI('${project.baseUri}')); file != null; file = file.getParentFile()) {
                                    if (!file.isDirectory()) {
                                        continue
                                    }

                                    java.io.File    testFile=new java.io.File(file, '.gitmodules')
                                    if (testFile.canRead()) {
                                        project.properties['insight.root.dir'] = file.getAbsolutePath()
										return
                                    }
                                }
                                
                                System.err.println("Failed to detected Insight root dir")
							]]>
                            </source>
                        </configuration>
                    </execution>
                    <execution>
                        <id>generate-timestamp-property</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>execute</goal>
                        </goals>
                        <configuration>
                            <source>
							<![CDATA[
                                project.properties['buildtimestamp'] = java.text.MessageFormat.format("{0,date,yyyyMMddHHmmss}", new Date())
							]]>
                            </source>
                        </configuration>
                    </execution>
                    <execution>
                        <id>resolve-distribution-details</id>
                        <phase>install</phase>	<!-- A pre-deploy phase - had it existed - would have been better -->
                        <goals>
                            <goal>execute</goal>
                        </goals>
                        <configuration>
								<!-- see http://maven.apache.org/ref/3.0.4/maven-core/apidocs/org/apache/maven/execution/MavenSession.html -->
                            <source>
							<![CDATA[
								if (!session.goals.contains("deploy")) {
									return;
								}

                                if ('${project.version}'.contains("SNAPSHOT")) {
                                    session.currentProject.distributionManagement.snapshotRepository.id = project.properties['snapshot.repo.id']
                                    session.currentProject.distributionManagement.snapshotRepository.name = project.properties['snapshot.repo.name']
                                    session.currentProject.distributionManagement.snapshotRepository.url = project.properties['snapshot.dist.url']

									project.properties['deploy.repo.id'] = project.properties['snapshot.repo.id']
									project.properties['deploy.repo.name'] = project.properties['snapshot.repo.name']
									project.properties['deploy.repo.url'] = project.properties['snapshot.dist.url']
                                } else if ('${project.version}'.contains("RELEASE") || '${project.version}'.contains(".SR")) {
                                    session.currentProject.distributionManagement.repository.id = project.properties['release.repo.id']
                                    session.currentProject.distributionManagement.repository.name = project.properties['release.repo.name']
                                    session.currentProject.distributionManagement.repository.url = project.properties['release.dist.url']

									project.properties['deploy.repo.id'] = project.properties['release.repo.id']
									project.properties['deploy.repo.name'] = project.properties['release.repo.name']
									project.properties['deploy.repo.url'] = project.properties['release.dist.url']
                                } else {
                                    session.currentProject.distributionManagement.repository.id = project.properties['milestone.repo.id']
                                    session.currentProject.distributionManagement.repository.name = project.properties['milestone.repo.name']
                                    session.currentProject.distributionManagement.repository.url = project.properties['milestone.dist.url']

									project.properties['deploy.repo.id'] = project.properties['milestone.repo.id']
									project.properties['deploy.repo.name'] = project.properties['milestone.repo.name']
									project.properties['deploy.repo.url'] = project.properties['milestone.dist.url']
                                }
							]]>
                            </source>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-source-plugin</artifactId>
				<version>2.1.2</version>
				<executions>
					<execution>
						<id>attach-sources</id>
						<phase>verify</phase>
						<goals>
							<goal>jar-no-fork</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>2.5.1</version>
                <configuration>
                    <source>${java.source.version}</source>
                    <target>${java.target.version}</target>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>2.3</version>
                <executions>
                    <execution>
                        <id>install</id>
                        <phase>install</phase>
                        <goals>
                            <goal>sources</goal>
                        </goals>
                        <configuration>
                            <markersDirectory>${settings.localRepository}/com/springsource/insight/dependency-maven-plugin-markers</markersDirectory>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>2.4</version>
                <configuration>
                    <archive>
                        <addMavenDescriptor>true</addMavenDescriptor>
						<!-- Generate index file for quicker access -->
						<index>true</index>
						<!-- Extra attributes added to the MANIFEST.MF file -->
						<manifestEntries>
							<!-- see Package constructor using Manifest -->
							<Implementation-Title>${project.artifactId}</Implementation-Title>
							<Implementation-Version>${project.version}</Implementation-Version>
							<Implementation-Vendor>Insight</Implementation-Vendor>
							<Release-Timestamp>${buildtimestamp}</Release-Timestamp>
						</manifestEntries>
                    </archive>
                    <excludes>
                        <exclude>**/.gitignore</exclude>
                        <exclude>**/builddef.*</exclude>
                    </excludes>
                </configuration>
            </plugin>
            <plugin>
              <groupId>org.apache.maven.plugins</groupId>
              <artifactId>maven-surefire-plugin</artifactId>
              <version>${surefire.plugin.version}</version>
              <configuration>
					<runOrder>alphabetical</runOrder>
					<includes>
						<include>**/*Test.java</include>
					</includes>
					<excludedGroups>${excluded.tests.group}</excludedGroups>
                  <forkMode>once</forkMode><!-- at least once -->
                        <!-- avoid inherent multi-threaded tests -->
                  <perCoreThreadCount>false</perCoreThreadCount>
                  <threadCount>1</threadCount>
				  <systemPropertyVariables>
						<java.io.tmpdir>${project.build.directory}${file.separator}temp</java.io.tmpdir>
						<java.awt.headless>true</java.awt.headless>
				  </systemPropertyVariables>
              </configuration>
					<!-- need to include it to ensure grouping detection -->
			  <dependencies>
				 <dependency>
					<groupId>org.apache.maven.surefire</groupId>
					<artifactId>surefire-junit47</artifactId>
					<version>${surefire.plugin.version}</version>
				 </dependency>
			  </dependencies>
            </plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-deploy-plugin</artifactId>
                <version>2.7</version>
					<!-- NOTE: by default we do not deploy anything not compiled in 1.5
					   - see special profile activate for 1.5 -->
				<configuration>
					<skip>true</skip>
				</configuration>
			</plugin>            
        </plugins>
    </build>

    <reporting>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-project-info-reports-plugin</artifactId>
                <version>2.4</version>
                <reportSets>
                    <reportSet>
                        <reports>
                        </reports>
                    </reportSet>
                </reportSets>
            </plugin>
        </plugins>
    </reporting>

    <scm>
        <connection>scm:git:ssh://git@github.com/SpringSource/spring-insight-plugins.git</connection>
        <url>scm:git:ssh://git@github.com/SpringSource/spring-insight-plugins.git</url>
        <developerConnection>scm:git:ssh://git@github.com/SpringSource/spring-insight-plugins.git</developerConnection>
    </scm>


        <!-- Note: these values are placeholders - the real values are
            set by a gmaven-plugin execution bound to the 'install' phase
            provided a 'deploy' goal has been specified in the Maven
            invocation commmand -->
    <distributionManagement>
        <repository>
            <id>insight-releases</id>
            <name>Spring Insight Releases Repository</name>
            <url>http://localhost:8081/artifactory/libs-releases-local</url>
        </repository>    
        <snapshotRepository>
            <id>insight-snapshots</id>
            <name>Spring Insight Snapshots Repository</name>
            <url>http://localhost:8081/artifactory/libs-snapshots-local</url>
        </snapshotRepository>
    </distributionManagement>
</project>
